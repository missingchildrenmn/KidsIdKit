@page "/childSocialMediaAccounts/{Id:int}"

@using Microsoft.AspNetCore.Components.QuickGrid;
@using KidsIdKit.Core.Data
@using KidsIdKit.Core.Services

@inject IFamilyStateService FamilyState
@inject NavigationManager NavigationManager

@inherits PageBase

@{
base.BuildRenderTree(__builder);
}

<ion-content class="ion-padding">

@if (CurrentChild == null || SocialMediaAccountObjects == null)
{
    <ion-card>
        <ion-card-content class="ion-text-center">
            <ion-spinner></ion-spinner>
            <p>Loading...</p>
        </ion-card-content>
    </ion-card>
}
else
{
    <ion-card>
        <ion-card-header>
            <ion-card-title>@CurrentChild.FullName</ion-card-title>
        </ion-card-header>
    </ion-card>

    @if (SocialMediaAccountObjects != null && SocialMediaAccountObjects.Count() > 0)
    {
        @foreach (var socialMediaAccount in SocialMediaAccountObjects)
        {
    <ion-card>
        <ion-card-content>
            <ion-grid>
                <ion-row>
                    <ion-col size="4" size-sm="3">
                        <ion-label>Platform:</ion-label>
                    </ion-col>
                    <ion-col>
                        <ion-text>
                            <p>@socialMediaAccount.Platform</p>
                        </ion-text>
                    </ion-col>
                </ion-row>
                <ion-row>
                    <ion-col size="4" size-sm="3">
                        <ion-label>Username:</ion-label>
                    </ion-col>
                    <ion-col>
                        <ion-text>
                            <p>@socialMediaAccount.UserName</p>
                        </ion-text>
                    </ion-col>
                </ion-row>
                <ion-row>
                    <ion-col size="4" size-sm="3">
                        <ion-label>Password:</ion-label>
                    </ion-col>
                    <ion-col style="display: flex; align-items: center;">
                        @if (passwordVisibilityManager.IsPasswordVisible(socialMediaAccount.Id))
                        {
                            <ion-text>
                                <span>@socialMediaAccount.Password</span>
                            </ion-text>
                            <ion-button fill="clear" size="small" @onclick="() => passwordVisibilityManager.HidePassword(socialMediaAccount.Id)" title="Hide password">
                                <ion-icon slot="icon-only" name="eye-off"></ion-icon>
                            </ion-button>
                        }
                        else
                        {
                            <ion-text>
                                <span>••••••••</span>
                            </ion-text>
                            <ion-button fill="clear" size="small" @onclick="() => passwordVisibilityManager.ShowPassword(socialMediaAccount.Id)" title="Show password">
                                <ion-icon slot="icon-only" name="eye"></ion-icon>
                            </ion-button>
                        }
                    </ion-col>
                </ion-row>
            </ion-grid>
        </ion-card-content>
        <div style="margin-top: 1rem; text-align: right;">
            <ion-button fill="clear" color="primary" href="/SocialMediaAccount/@Id/@socialMediaAccount.Id" title="Edit">
                <ion-icon slot="icon-only" name="pencil"></ion-icon>
            </ion-button>
            <ion-button fill="clear" color="danger" href="/SocialMediaAccountRemove/@Id/@socialMediaAccount.Id" title="Delete">
                <ion-icon slot="icon-only" name="trash"></ion-icon>
            </ion-button>
        </div>
    </ion-card>
        }

        @if (passwordVisibilityManager.VisiblePasswordCount > 0)
        {
    <ion-button expand="block" color="secondary" @onclick="() => passwordVisibilityManager.HideAllPasswords()">
        <ion-icon slot="start" name="eye-off"></ion-icon>
        Hide All Passwords
    </ion-button>
        }
    }

    <div class="ion-padding">
        <ion-button expand="block" color="success" @onclick="@(() => NavigationManager.NavigateTo($"/SocialMediaAccount/{Id}/-1"))">
            <ion-icon slot="start" name="add-circle"></ion-icon>
            Add Social Media Account
        </ion-button>
    </div>
}
</ion-content>
